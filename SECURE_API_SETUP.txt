FINAL SECURE API SETUP

1. Deploy this ZIP to your Netlify site.
2. Netlify -> Site configuration -> Environment variables.
3. Add:
   NETLIFY_API_KEY = your API-Football key
4. Redeploy.

The app no longer asks you to enter an API key and does not store the key in localStorage.
The browser calls the Netlify function; the function adds the secret server-side.

IMPORTANT: regenerate any API key that was exposed in chat, then use the new key here.

FIX INCLUDED: recent team history now uses the supported team+last fixture query and filters completed matches locally, preventing the per-fixture Data error caused by the previous query combination.
